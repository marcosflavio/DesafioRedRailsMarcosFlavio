<!DOCTYPE html>
<html>
  <head>
    <title>Redwitter</title>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
  </head>
  <body>

    <%# Incluindo o navbar ao layout da aplicação %>
    <%# Navbar global! %>
    <%= render '/layouts/navbar' %>
    
     <% if  params[:id] != nil %>
      <% @urlUser = '/user/' + params[:id] %>
    
      <% if current_page?(@urlUser) %>
        <img src="/logo.png" class="profile-banner panel-dark">
        
        <div class = "row ">
          
         <div class="col-sm-12" style="background-color: white;">
            <div class="container">
              <div class="col-sm-3">&nbsp;</div>
              <div class="col-sm-6" style="margin-top: 5px">
                <div class="col-sm-2" style="font-size: 100%; opacity: 0.7;">REDWEETS<br>
                    <h6 style="color: #0084B4; font-size: 150%;"><%= User.find_by_username(params[:id]).redweet.count %></h6></div>
                <div class="col-sm-2" style="font-size: 100%; opacity: 0.7;">SEGUIDORES<br>
                    <h6 style="color: #0084B4; font-size: 150%;"><%= User.find_by_username(params[:id]).followers.count %></h6></div>
                <% if user_signed_in? %>
                
                  <div class="col-sm-8" style="text-align: right; margin-top: 5px;"><a href="/users/edit"><li type="button" class="btn btn-default" style="color: grey; opacity: 0.8; font-weight: bold;">Editar Conta</li></a></div>
                <% end %>
              </div>
            </div>
          </div>
        </div>
        
      <% end %>
    <% end %>
     
    <div class="container">
    <span class="notice"><%= notice %></span>
    <span class="alert"><%= alert %></spanp>
      <%# Renderizo uma determinada página aqui neste layout%>
      <%= yield %>

    </div>

  </body>
</html>
